---
import AboutUsPage from '../about-us.astro';

export function getStaticPaths() {
  const locales = ['es', 'fr', 'de', 'pt', 'ar', 'ru', 'it', 'ja', 'ko', 'id'];
  return locales.map(locale => ({
    params: { locale }
  }));
}
---
<AboutUsPage />
