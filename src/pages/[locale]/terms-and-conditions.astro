---
import TermsAndConditionsPage from '../terms-and-conditions.astro';

export function getStaticPaths() {
  const locales = ['es', 'fr', 'de', 'pt', 'ar', 'ru', 'it', 'ja', 'ko'];
  return locales.map(locale => ({
    params: { locale }
  }));
}
---
<TermsAndConditionsPage />
